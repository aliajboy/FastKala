@model ProductBrandsViewModel

@foreach (var item in Model.Brands)
{
    <tr>
        <td>
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
            <br>
            <form method="post" class="d-inline">
                <a href="#" onclick="changeEditBrand(this.id,@item.Id)" id="editBrandButton">ویرایش</a> |
                <button asp-route-id="@item.Id" asp-action="RemoveBrand" asp-controller="Products" class="btn p-0 text-decoration-none remove-product">حذف</button>
            </form>
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Link)
        </td>
    </tr>
}