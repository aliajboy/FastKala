@model ProductCategoriesViewModel

@foreach (var item in Model.Categories)
{
    <tr>
        <td>
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
            <br>
            <form method="post" class="d-inline">
                <a href="#" onclick="changeEditCategory(this.id,@item.Id)" id="editCategoryButton">ویرایش</a> |
                <button asp-route-id="@item.Id" asp-action="RemoveCategory" asp-controller="Products" class="btn p-0 text-decoration-none remove-product">حذف</button>
            </form>
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Link)
        </td>
    </tr>
}